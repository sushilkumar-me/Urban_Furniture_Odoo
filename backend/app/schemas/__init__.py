# ============================================================
# schemas/__init__.py
#
# WHY THIS FILE EXISTS:
#   Registers the "schemas" folder as a Python package.
#   This allows other files to import our pydantic schemas with:
#       from app.schemas.invoice import InvoiceCreate
#
#   Schemas define what data looks like coming IN (from the user)
#   and going OUT (to the user).
#   We will add schemas in Phase 3.
# ============================================================
